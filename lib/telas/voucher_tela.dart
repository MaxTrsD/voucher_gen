import 'package:flutter/material.dart';

class tela_voucher extends StatelessWidget {
  const tela_voucher({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
